#include <iostream>
#include <cmath>


int main()
{
    /*
    int h;

    std::cin >> h;

    int x = h * 2 - 1;
    for (int i = h; i > 0; i--)
    {
        
        for (int j = i; j > 0; j--)
        {
            std::cout << ' ';
        }
        for (int j = 0; j < x; j++)
        {
            std::cout << '*';
        }
        std::cout << std::endl;
        x += 2;
    }
    */

/*

    int n;
    std::cin >> n


    for (int i = n - 1; i >= 0; --i)
    {
        for (int j = 1; j <= i; ++j)
        {
            std::cout << ' ';
        }
        for (int j = 0; j < n - 1; ++j)
        {
            std::cout << 'x';
        }
        std::cout << '\n';
    }
*/


    int n;
    std::cin >> n;

    for (int i = n - 1; i >= 0; --i)
    {
        std::cout << i << ' '
                  << 1 << 
    }
    
    return 0;
}
