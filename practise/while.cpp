#include <iostream>
#include <iomanip>
#include <cmath>
#include <cstdlib>
#include <ctime>

int main()
{
    /*int n = 10000;
    double s = 0;
    double s1;
    double error;
    std::cin >> errl;

    for (int i = 1; i <= n; ++i)
    {
        s += 1 / (double(i));
        
    }
    double n = 0;
    if (fabs(s1) < error)
    {
        break;
    }
    std::cout << s << ' ' << s1 << fabs(s-s1) << '\n';
    /*/
    return 0;
}
